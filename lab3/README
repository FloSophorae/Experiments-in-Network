程序编译方法
    执行 make 或者 make all指令，同时对客户端程序和服务器程序进行编译
    执行make client只对客户端程序进行编译
    执行make server只对服务器程序进行编译
    执行make clean清楚非必须的文件

程序运行方法
    执行 ./server 运行服务器程序
    执行 ./client 运行客户端程序
    对于客户端程序，执行之后进入页面，会显示而已执行的所有指令，或者输入--help或者-h查看指令的提示信息。
